import React from 'react'
import TextInput from '../input/text-input'

const FormItem = (props) => {
    let { type, text, placeholder, onChange } = props
    debugger
    return <div className="rui-form">
        {}
    </div>
}

export default TextInput